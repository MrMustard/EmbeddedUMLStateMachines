/*Primer programa de maquina de estados basado en una maquina de estados uml */

#include "main.h"

/*Main aplication objet*/
protimer_t protimer; // esta es la varaible (Estructura u objeto de protimer que inicializaremos)

void setup() {

//inicializamos el objeto de protimer
protimer_init(&protimer);

}

void loop() {
  // put your main code here, to run repeatedly:
 
}